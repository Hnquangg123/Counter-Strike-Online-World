# Strict peer resolution keeps the dependency graph honest; auto-install-peers
# fills in the usual React/Next peers for the workspace.
auto-install-peers=true
strict-peer-dependencies=false
# Faster CI installs; the lockfile is the source of truth.
prefer-frozen-lockfile=true
