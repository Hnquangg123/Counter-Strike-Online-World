{
  "name": "@csow/ingest",
  "version": "0.1.0",
  "private": true,
  "description": "Ingestion pipeline: Counter-Strike Online Wiki (MediaWiki API) → normalized JSON + media for seeding the world.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "tsx src/cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@csow/schema": "workspace:*",
    "sharp": "0.34.2",
    "wtf_wikipedia": "^10.4.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
