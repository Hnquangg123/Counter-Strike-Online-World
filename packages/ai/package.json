{
  "name": "@csow/ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI pipelines for the world: lore enrichment and translation (Claude), character & weapon art (OpenAI / Google / fal), 3D figures, embeddings.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "tsx src/cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^4.0.49",
    "@ai-sdk/fal": "^3.0.37",
    "@ai-sdk/google": "^4.0.64",
    "@ai-sdk/openai": "^4.0.60",
    "@ai-sdk/replicate": "^3.0.37",
    "@csow/schema": "workspace:*",
    "@fal-ai/client": "^1.10.1",
    "ai": "^7.0.93",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
